// X / Twitter card image — same generator as opengraph-image.tsx.
// Re-exporting keeps the two in sync so a Twitter share card looks
// identical to a Discord/Slack/iMessage preview.

export { default, alt, size, contentType } from "./opengraph-image";
